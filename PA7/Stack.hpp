#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <chrono>
#include <ctime>
#include <sstream>

using std::cin;
using std::cout;

using std::ifstream;
using std::ofstream;
using std::fstream;
using std::string;
using std::vector;


class Stack {
public:

	void push(string Date) {

		//inserting
	} 

	string peek(void) {

		//get top of stack without deleting
	}

	bool isEmpty(void)

private:

	vector<string> dates;

};
